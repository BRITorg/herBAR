[project]
name = "herbar"
version = "0.1.0"
description = "A barcode renamer for herbarium specimens"
requires-python = ">=3.10"
dependencies = [
    "Pillow==12.3.0",
    "pyzbar==0.1.9",
    "tqdm==4.70.1",
]

[dependency-groups]
dev = [
    "pytest",
]
